# Minimal requirements for A2A Protocol Server
# Ultra-lightweight for 512MB RAM constraint

# Core web framework (lightweight)
starlette==0.37.2

# ASGI server (without extras to save memory)
uvicorn==0.30.1

# HTTP client for Julia communication
httpx==0.27.0
